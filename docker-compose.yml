version: '3.8'

services:
  selma-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: selma-transit-app
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
